/beadsboard
/dist/
*.log
.DS_Store

# local scratch notes, not committed
/notes/

# beads issue tracker — kept local, not committed
/.beads/
# beadsboard's own local config (per-repo override; no secrets — token from env)
/.beadsboard/
/.agents/
/.codex/
/.claude/
/AGENTS.md
/CLAUDE.md
